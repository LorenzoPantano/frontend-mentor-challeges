const BASE_URL = "https://restcountries.com/v2/";

const ENDPOINTS = {
	all: BASE_URL + "all",
	name: BASE_URL + "name/",
	region: BASE_URL + "region/",
	code: BASE_URL + "alpha/",
};

export default ENDPOINTS;
